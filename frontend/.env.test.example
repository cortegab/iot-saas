# Copy to .env.test (gitignored) and fill in a dev account's credentials.
# Used only by the Playwright E2E suite (frontend/e2e) to log in and to
# create/clean up its own fixture rule via the API.
E2E_EMAIL=
E2E_PASSWORD=
